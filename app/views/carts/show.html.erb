<p id="notice"><%= notice %></p>

  <h2> Panier </h2>

  <ul>
      <% cart.line_items.each do |items| %>
        <li> <%= item.picture.name %> </li>
      <%end%>

  </ul>

<%= link_to 'Back', root_path %>
