<p> Voici votre commande </p>

<ul>
    <% @user.cart.line_items.each do |item| %>
      <li > <%= item.picture.name %> x <%= item.quantity %>
        <p>Prix unitaire = <%= number_to_currency(item.total, unit: "€", separator: ",", format: "%n %u") %></p>
      </li>
    <%end%>

    <h6> Total = <%= number_to_currency(@user.cart.total, unit: "€", separator: ",", format: "%n %u") %> </h2>

</ul>

<p> Merci de votre achat </p>
